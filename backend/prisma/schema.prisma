generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Poll {
  id          String     @id @default(uuid()) @db.Uuid
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  name        String
  creatorId   String     @db.Uuid
  adminLink   String
  pollLink    String
  resultLink  String
  isActive    Boolean    @default(true)
  creator     User       @relation("PollUser", fields: [creatorId], references: [id])
  questions   Question[] @relation("PollQuestions")
  answerCount Int        @default(0)
}

model Question {
  id           String     @id @default(uuid()) @db.Uuid
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
  pollId       String     @db.Uuid
  typeId       String     @db.Uuid
  maxValue     Float?
  minValue     Float?
  parentId     String?    @db.Uuid
  step         Float?
  typeName     String     @default("free")
  visualType   String     @default("default")
  description  String     @default("")
  title        String     @default("")
  parent       Question?  @relation("SubQuestions", fields: [parentId], references: [id])
  poll         Poll       @relation("PollQuestions", fields: [pollId], references: [id])
  type         Type       @relation("QuestionType", fields: [typeId], references: [id])
  options      Option[]   @relation("QuestionOptions")
  subQuestions Question[] @relation("SubQuestions")
  votes        Vote[]     @relation("QuestionVotes")
}

model Type {
  id        String     @id @default(uuid()) @db.Uuid
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  type      String
  questions Question[] @relation("QuestionType")
}

model Option {
  id         String   @id @default(uuid()) @db.Uuid
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  questionId String   @db.Uuid
  option     String
  question   Question @relation("QuestionOptions", fields: [questionId], references: [id])
}

model Vote {
  id         String   @id @default(uuid()) @db.Uuid
  createdAt  DateTime @default(now())
  questionId String   @db.Uuid
  value      String
  voterId    String   @db.Uuid
  parentId   String?  @db.Uuid
  parent     Vote?    @relation("SubVotes", fields: [parentId], references: [id])
  question   Question @relation("QuestionVotes", fields: [questionId], references: [id])
  subVotes   Vote[]   @relation("SubVotes")
}

model User {
  id            String   @id @default(uuid()) @db.Uuid
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  name          String
  email         String
  emailVerified Boolean  @default(false)
  password      String
  polls         Poll[]   @relation("PollUser")
}
